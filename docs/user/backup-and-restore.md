# Backup & Restore

Create a ZIP backup from **Export & Backup**. It contains SQLite and a manifest, but no AI API keys. Restore validates the manifest/database and creates a safety backup before replacement. Keep backups encrypted if your behavioural data is sensitive.
