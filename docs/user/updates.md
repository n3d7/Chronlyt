# Updates

Chronlyt uses the official Tauri 2 updater. Startup checks are optional and fetch only release metadata. Available versions and notes are shown before any download; installation requires an explicit click. Tauri verifies the artifact signature and refuses invalid updates. Offline/errors never disable local features.

Because the current GitHub repository is private, Releases and updater metadata require authorized GitHub access until the owner separately changes visibility.
