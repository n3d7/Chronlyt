# Data Export

Data Export works offline and independently from AI. Choose any date range, data categories, Summary/Standard/Full detail and Markdown, JSON or both. JSON uses the versioned `app.chronlyt.export` schema v1 so future readers can distinguish formats.
